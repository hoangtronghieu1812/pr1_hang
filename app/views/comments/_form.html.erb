<%= form_for [@cosmetic, @comment] do |f| %>
  <%= f.text_field :content, class: "form-control", placeholder: t(".add_comment") %>
  <%= f.hidden_field :cosmetic_id, value: @cosmetic.id %>
  <%= f.submit t(".comment"), class: "btn-comment" %>
<% end %>
